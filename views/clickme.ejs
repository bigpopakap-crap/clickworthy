<!DOCTYPE html>
<html>
<head>
	<!-- TODO -->
</head>
<body>
    <h1><%= pageTitle %></h1>
	<iframe type="text/html"
        src="https://www.youtube.com/embed/<%= yid %>?enablejsapi=1"
        frameborder="0">
    </iframe>
    <p><%= pageDesc %></p>

    <div>
        <%= relTitle %>
        <div style="display:flex; flex-direction:row;">
            <% for (var i = 0; i < related.length; i ++) { %>
                <div style="width: <%= related[i].thumb.width %>px; height: <%= related[i].thumb.height %>px; background-image: url('<%= related[i].thumb.url %>'); color: white; margin-right: 8px;">
                    <p><%= related[i].title %></p>
                </div>
            <% } %>
        </div>
    </div>

    <h3>
        <a href="/">Make your own</a>
    </h3>
</body>
</html>