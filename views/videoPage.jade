doctype html
html
head
    <!-- TODO -->
body
    h1 #{pageTitle}
    iframe(type="text/html",
           src="https://www.youtube.com/embed/#{yid}?origin=#{siteOrigin}"
           frameborder="0")
    p #{pageDesc}

    div
        h2 #{relTitle}
        div(style="display:flex; flex-direction:row;")
            - for (var i = 0; i < related.length; i++)
                div(style="width: #{related[i].thumb.width}px; height: #{related[i].thumb.height}px; background-image: url('#{related[i].thumb.url}'); color: white; margin-right: 8px;")
                    p #{related[i].title}

    h3
        a(href="/") Make your own